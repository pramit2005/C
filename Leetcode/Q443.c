#include<stdio.h>
#include<stdlib.h>
int HowMany(char* str,char c,int ind){
    int count=0,i;
    while(str[ind]!='\0'){
        if(str[ind]!=str[ind+1])//what if ind is the last index?
    }
}
int compress(char* chars, int charsSize) {
    
}